# 🚀 УСТАНОВКА HEALTH TRACKER

## 📋 ЧТО ПОНАДОБИТСЯ

1. **Python 3.8+** — https://www.python.org/
2. **Git** — https://github.com/psyx0dw/lab_flask_bmi
3. **Текстовый редактор** (VS Code, Sublime и т.д.)

---

## 🚀 БЫСТРАЯ УСТАНОВКА (5 минут)

### Шаг 1: Клонируй репо

```bash
git clone https://github.com/psyx0dw/health-tracker.git
cd health-tracker
```

### Шаг 2: Создай виртуальное окружение

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установи зависимости

```bash
pip install -r requirements.txt
```

### Шаг 4: Запусти приложение

```bash
python app.py
```

### Шаг 5: Открой браузер

```
http://localhost:5000
```

**🎉 Готово!** Приложение работает!

---

## 📝 СТРУКТУРА ФАЙЛОВ

Распредели файлы так:

```
health-tracker/
├── app.py                      ← Главный файл
├── requirements.txt            ← Зависимости
├── templates/
│   └── index.html             ← HTML (в папке templates/)
└── health_tracker.db          ← создастся автоматически
```

**ВАЖНО:** HTML файл ДОЛЖЕН быть в папке `templates/`!

---

## 🐛 ЕСЛИ ОШИБКА

### "ModuleNotFoundError: No module named 'flask'"

```bash
pip install Flask
# Или переустанови все:
pip install -r requirements.txt
```

### "Address already in use: port 5000"

```bash
# Порт 5000 занят, используй другой:
python app.py --port 8000
# Откроется на http://localhost:8000
```

### "No such table: weight_records"

```bash
# Удали БД и создась заново:
rm health_tracker.db
python app.py
```

### "Templates not found"

```bash
# Убедись, что index.html в папке templates/:
health-tracker/
├── app.py
├── templates/
│   └── index.html    ← СЮДА!
```

---

## 🌐 ДЛЯ ДРУГИХ КОМПЬЮТЕРОВ

Если хочешь, чтобы другие люди подключились к твоему приложению:

```bash
# Вместо localhost используй:
python app.py --host 0.0.0.0 --port 5000

# Тогда другие смогут открыть:
http://твой_ip:5000
```

---

## 📦 requirements.txt

Если нет файла `requirements.txt`, создай вручную:

```
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
Click==8.1.7
gunicorn==21.2.0
```

Или сгенерируй из текущего окружения:

```bash
pip freeze > requirements.txt
```

---

## 🎯 ПЕРВЫЕ ШАГИ В ПРИЛОЖЕНИИ

1. **Добавь запись о весе:**
   - Выбери дату
   - Введи вес в кг
   - Введи рост в см
   - Нажми "Добавить запись"

2. **Установи цель:**
   - Введи целевой вес
   - Выбери дату достижения
   - Нажми "Установить цель"

3. **Смотри график:**
   - Вкладка "График" показывает линию прогресса
   - Нажми на точку для подробностей

4. **Получи рекомендации:**
   - Вкладка "Рекомендации" показывает советы
   - Советы зависят от твоего ИМТ

5. **Экспортируй данные:**
   - Кнопка "Экспортировать в CSV"
   - Скачается файл с историей

---

## 🚀 ЗАГРУЗКА НА GITHUB

Когда готово к публикации:

```bash
git init
git add .
git commit -m "Initial commit: Health Tracker"

# На GitHub создай репо: health-tracker
git remote add origin https://github.com/psyx0dw/health-tracker.git
git branch -M main
git push -u origin main
```

---

## 🌐 РАЗВЁРТЫВАНИЕ НА HEROKU

```bash
# 1. Установи Heroku CLI
# https://devcenter.heroku.com/articles/heroku-cli

# 2. Логин
heroku login

# 3. Создай приложение
heroku create health-tracker-psyx0dw

# 4. Загрузи
git push heroku main

# 5. Открой
heroku open
```

---

## ✅ ЧЕКЛИСТ

- [ ] Python 3.8+ установлен
- [ ] Git установлен
- [ ] Репо клонировано
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] HTML файл в папке templates/
- [ ] Приложение запускается без ошибок
- [ ] Открывается на http://localhost:5000
- [ ] Форма работает (добавляешь запись)
- [ ] График отображается
- [ ] Рекомендации выводятся

---

## 📞 ПОМОЩЬ

Если есть вопросы:

1. Проверь консоль на ошибки
2. Гугли ошибку + "Flask"
3. Создай Issue на GitHub
4. Напиши в Telegram

---

**Удачи! 🎉**

Good luck! ☕💻
